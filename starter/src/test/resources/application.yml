spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/fd_emall?serverTimezone=Asia/Shanghai
    username: root
    password: 123Qaz!23

  redis:
    host: 127.0.0.1
    port: 6379
    password:
    database: 0
allone:
  aop:
    pointcut:
      api-log: "execution(public * io.github.planegg.allone.starter.*.*(..))"
  starter:
    swagger:
      enabled: true
